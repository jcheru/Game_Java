import processing.core.PImage;
import java.util.List;

public class Ship
    extends Obstacle
{
    public Ship(String name, Point position, List<PImage> imgs)
    {
        super(name, position, imgs);
    }
}
